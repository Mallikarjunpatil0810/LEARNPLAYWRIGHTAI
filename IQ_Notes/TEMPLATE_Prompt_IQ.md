# My Question Prompt Template

Copy and paste this template, replace the placeholders, and send it to me:

---

```
Explain the difference between [Concept A], [Concept B], and [Concept C] in [Language/Topic] with examples.

Create a markdown file in IQ_Notes/ named [ShortName_IQ.md] with:
1. A comparison table with columns for each concept
2. Real code examples using a file from this project if possible
3. A visual pipeline or flow diagram
4. A key takeaway table at the end

Use the same format/style as Source_Byte_Binary_Code_IQ.md.
```

---

## Example

If you send me this:

> Explain the difference between **HTTP GET, POST, PUT, DELETE** methods with examples.  
> Create a markdown file named `HTTP_Methods_IQ.md` in `IQ_Notes/`.

I will create `IQ_Notes/HTTP_Methods_IQ.md` with:

- A table comparing each method (purpose, idempotent, safe, body, cacheable)
- Real code examples (fetch API, Node.js/Express snippets)
- A visual request-response flow diagram
- A key takeaway summary table
- Same formatting style as `Source_Byte_Binary_Code_IQ.md`
